{
  "name": "adaptive-learning-tutor-gateway",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Solaris tutor API on Google Gemini (the key stays on the server; the page gets short-lived voice tokens) and static WebGL host for the Unity Solar System lesson. No dependencies.",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
